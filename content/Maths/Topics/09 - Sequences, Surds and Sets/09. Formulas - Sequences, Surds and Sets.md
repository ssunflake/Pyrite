# 09. Formulas & Quick Reference — Sequences, Surds and Sets

[[09. Notes - Sequences, Surds and Sets|← Notes]] | [[00. Maths Index|Index]]

---

## Sequences

```
Arithmetic (linear):
  nth term  =  a + (n−1)d  =  dn + (a−d)
  where a = first term,  d = common difference

Geometric:
  nth term  =  a × rⁿ⁻¹
  where a = first term,  r = common ratio

Quadratic:
  nth term  =  an² + bn + c
  2nd difference  =  2a
```

## Special nth Terms

```
Square numbers:      n²
Cube numbers:        n³
Triangular numbers:  n(n+1)/2
```

---

## Surds

```
√(ab) = √a × √b
√(a/b) = √a / √b
√a × √a = a
(√a + √b)(√a − √b) = a − b   [difference of squares]

Simplify: find largest perfect-square factor
  e.g. √72 = √(36×2) = 6√2

Rationalise simple:    n/√a  →  multiply by √a/√a  →  n√a/a

Rationalise compound:  n/(a+√b)  →  multiply by (a−√b)/(a−√b)
  →  n(a−√b) / (a²−b)
```

---

## Sets — Key Notation

```
∈  element of       ∉  not element of
∅  empty set        ξ  universal set
∩  intersection     ∪  union
⊂  subset           A'  complement of A

n(A ∪ B) = n(A) + n(B) − n(A ∩ B)
```

## De Morgan's Laws

```
(A ∩ B)' = A' ∪ B'
(A ∪ B)' = A' ∩ B'
```

## Number of Subsets

```
Set with n elements  →  2ⁿ subsets  (including ∅ and full set)
```

[[09. Notes - Sequences, Surds and Sets|← Notes]] | [[00. Maths Index|Index]]
